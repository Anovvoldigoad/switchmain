P100B ONE-SHOT ACTION710 ROUTE SWEEP

Purpose: collapse remaining diagnosis into one runtime round.
Run order in ONE session:
1) vanilla char91 UJ until cinematic succeeds
2) custom Tobi UJ until failure after 708
3) send log + artifact

P100B traces: unique caller 0x488B24, native function entry/mode split, common path, C48 branch family, participant lookup/remap, and exact REQUEST710 at 0x7E6EA8.
It is read-only with respect to actor/action/state memory. It does NOT force 708 or 710 and has no char281 gameplay branch.

After this run the next build should be a fix candidate, not another broad diagnostic trace.
