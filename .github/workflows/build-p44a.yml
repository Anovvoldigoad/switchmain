name: Build P44A

on:
  push:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Note
        run: |
          echo "Copy overlay/ into your exlaunch project and build subsdk9 as usual."
          echo "This workflow is a placeholder - adapt to your existing P43A build pipeline."
