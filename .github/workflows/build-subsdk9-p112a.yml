name: P112A archived
on:
  workflow_dispatch:
    inputs:
      archived:
        description: "P112A had runtime fingerprint mismatch at 0x7DDFD0"
        required: false
        default: "1"
jobs:
  archived:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo archived
