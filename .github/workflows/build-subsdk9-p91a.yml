name: Historical build-subsdk9-p91a (manual only)
on:
  workflow_dispatch:
jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "build-subsdk9-p91a archived; current build is P93A."
